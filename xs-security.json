{
  "xsappname": "hello-world",
  "scopes": [
    {
      "name": "$XSAPPNAME.admin",
      "description": "Admin of the hello world app"
    },
    {
      "name": "$XSAPPNAME.user",
      "description": "End user of the hello world app"
    }
  ],
  "attributes": [],
  "role-templates": [
    {
      "name": "admin",
      "description": "A person that can create messages in the app",
      "scope-references": [
        "$XSAPPNAME.admin"
      ]
    },
    {
      "name": "user",
      "description": "A person that can retrieve messages from the app",
      "scope-references": [
        "$XSAPPNAME.user"
      ]
    }
  ]
}
